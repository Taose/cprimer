/*
 * The types that would be best used for a Mortgage payment are...
 *
 * rate: Float (typically on a precision of 2 is required)
 * Principal: Float (typically a precision of 2 is required)
 * Payment: Float (typically a precision of 2 is required)
 *
 * All are real numbers therefore some precision is required.
 * /
